"""WebSocket client: connect, subscribe (candles, book, trades, user), reconnect with backoff. TODO."""


def connect(url: str) -> None:
    """Connect to WS. TODO."""
    pass  # TODO


def subscribe(channel: str, params: dict) -> None:
    """Subscribe to channel. TODO."""
    pass  # TODO
