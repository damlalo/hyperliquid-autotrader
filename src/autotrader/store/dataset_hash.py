"""Deterministic hash of dataset (manifest + scope) for reproducibility. TODO."""


def compute_hash(manifest_path: str) -> str:
    """Return deterministic hash for manifest. TODO."""
    return ""  # TODO
