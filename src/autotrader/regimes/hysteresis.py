"""Hysteresis: hold regime for N bars before switching. TODO."""


def apply(current_regime: str, new_regime: str, state: dict) -> str:
    """Return effective regime after hysteresis. TODO."""
    return current_regime  # TODO
