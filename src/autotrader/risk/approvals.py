"""Gate checks: utility, MaxDD, CVaR, feasibility. TODO."""


def gates_passed(metrics: dict, baseline: dict, thresholds: dict) -> bool:
    """True if candidate passes all gates. TODO."""
    return False  # TODO
