"""Systematic leverage selection: stop distance, liquidation buffer, config max. TODO."""


def select_leverage(
    stop_distance_pct: float, buffer_pct: float, config_max: float
) -> float:
    """Return allowed leverage. TODO."""
    return 1.0  # TODO
