"""Portfolio and per-trade constraints (immutable). TODO."""


def check_portfolio(exposure_usd: float, caps: dict) -> bool:
    """True if within caps. TODO."""
    return True  # TODO


def check_trade(size_usd: float, leverage: float, caps: dict) -> bool:
    """True if trade within per-trade caps. TODO."""
    return True  # TODO
